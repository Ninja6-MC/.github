# Standards exceptions for Ninja6-MC/.github
#
# Schema and worked example: standards/README.md section 3.
# An empty `exceptions` list is a statement that this repository conforms; the file is
# required either way (N6-REPO-05).

repo: .github

# This repository has no exceptions: it conforms to every rule in the register.
#
# The transitional N6-CI-05 entry that lived here is closed. It existed because `main`
# was protected without any required status checks - the standards workflow did not exist
# yet, and requiring a check that never reports blocks every pull request forever
# (N6-CI-03). Both checks now report and are required.
exceptions: []
